# SmartProto — copy to `.env` / `.env.local` (never commit real secrets).

NEXT_PUBLIC_SITE_URL=https://www.smartproto.net
OPENROUTER_API_KEY=

# Chief / Staff Author door — primary PIN (server-side only; not NEXT_PUBLIC_*).
# Example format: two groups separated by hyphen (easy on phone).
SMARTPROTO_NEWS_PIN=098765-543210

# Legacy long token still accepted when set (optional while migrating).
# EDITORIAL_DOOR_SECRET=

ARTICLES_STORE=sqlite
SMARTPROTO_TRANSLATE_ENABLED=true
